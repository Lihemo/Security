//single line comments are delimited using the '//' signs in js. 
$.substitute () {
    var myValue = document.getElementById("mytextbox").value;

    if (myValue.length === 0) {
        alert("Please enter a real value then submit!!");
        return;
    }
}